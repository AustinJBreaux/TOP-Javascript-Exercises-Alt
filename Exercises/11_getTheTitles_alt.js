const books = [
    {
      title: 'Book',
      author: 'Name'
    },
    {
      title: 'Book2',
      author: 'Name2'
    }
]
let result = books.map(book => book.title);
let getTitle = function title(result){
    return result;
}